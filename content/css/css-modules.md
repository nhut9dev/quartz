---
title: CSS Modules
description: A CSS workflow that scopes class names to the importing component or file.
tags:
  - css
  - frontend
---

**CSS Modules** scope class names locally by generating unique class names at build time.

They let developers write ordinary CSS while avoiding global class collisions.

CSS Modules are common in component-based apps where each component imports its own stylesheet.
