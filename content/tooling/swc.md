---
title: SWC
description: A fast compiler for JavaScript and TypeScript written in Rust.
tags:
  - tooling
---

**SWC** is a fast compiler used for [[transpiler|transpilation]], [[minification]], and framework build pipelines.

It is written in Rust and is commonly chosen for speed.

Frameworks may use SWC internally so developers benefit from it without configuring it directly.
