---
title: CSS-in-JS
description: Styling approaches that define CSS through JavaScript or TypeScript.
tags:
  - css
  - frontend
---

**CSS-in-JS** defines styles from JavaScript or TypeScript code.

It can colocate styles with components, use runtime props, and integrate with theme objects.

Tradeoffs include runtime cost, build complexity, server rendering behavior, and differences from standard CSS workflows.
