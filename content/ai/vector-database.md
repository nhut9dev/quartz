---
title: Vector database
description: A database optimized for storing and searching embedding vectors.
tags:
  - ai
  - database
---

A **vector database** stores [[embedding|embeddings]] and supports similarity search.

It is often used to find documents, chunks, products, or examples related to a user query.

Vector search quality depends on chunking, embedding [[llm|model]], metadata filters, ranking, and freshness of indexed data.
